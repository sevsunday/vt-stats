# AGENTS.md

## Project

VT Stats — static-site dashboard for Battlezone: Combat Commander match statistics. Python pipeline processes raw protobuf session data into pre-computed JSON; static HTML/JS/CSS dashboard renders it.

## Before Making Any Change

1. Read the relevant rule file(s) from `.cursor/rules/`
2. Follow `DEVELOPER_GUIDE.md` for schema and architecture details
3. Never skip these — they prevent regressions and ensure consistency

## Rule Files

| File | Scope | When to read |
|------|-------|-------------|
| `project-overview.mdc` | Always applied | Architecture, data flow, file locations |
| `data-schema.mdc` | py, js, json files | Proto schema, damage semantics, pipeline output format |
| `styling.mdc` | html, css, js files | Bootstrap-first, `--kb-*` theme variables, `--vt-*` effect variables, Geist font, load order, tab architecture |
| `schema-migration.mdc` | proto, py files | Step-by-step playbook for adapting to proto/schema changes |
| `filter-contract.mdc` | py, js files | Client-side global filter contract and checklist for new pipeline output fields |

## Deep Reference

- `DEVELOPER_GUIDE.md` — full technical specification: protobuf schema with field tables, damage event semantics, ODF weapon resolution, pre-computed JSON structure with examples, styling standards, chart architecture, tab navigation architecture
- `README.md` — project overview, quick start, features list
- `scripts/statsgate.proto` — **definitive reference** for the current (v2) raw data schema
- `scripts/statsgate_v1.proto` — frozen verbatim snapshot of the legacy schema. Used only by the dual-descriptor fallback path in `scripts/process_stats.py::load_session` and `js/raw-browser.js::fetchAndDecodeBinpb` so pre-Nomad sessions still decode byte-identically. Do not edit; regenerate the v1 pb2 + protobufjs JSON only if backporting a fix
- `statsgate/` — upstream collector source code for schema reference
- `css/vtstats-theme.css` — premium glassmorphic theme layer: glass surfaces, Geist typography, depth shadows, animations, migrated inline styles, `--vt-*` effect variables
- `js/vtstats-fx.js` — effects engine: animated counters, staggered entrances, tab-entrance hooks, Chart.js shadow plugin, preloader lifecycle, View Transition API
- `js/timeline-player.js` — replay tab engine: animated Chart.js playback of `timeline` damage data with transport controls (play/pause/step/scrub/speed), running leaderboard, faction tug-of-war, bucket spotlight, momentum chip, and kill-marker plugin overlay. Exposes `window.VTReplay = { init, destroy, renderFullscreenSnapshot, hasInstance, jumpToTick }`; `jumpToTick(tick)` seeks the playhead to the bucket containing the tick (paused) — used by cross-links from the Raw Data Browser's events-table via `?t=<tick>` on `index.html`.
- `js/positioning-charts.js` — Positioning tab renderers: Movement Leaderboard, distance-from-spawn line chart, combined top-down heatmap, per-player small-multiples heatmap grid (shared viewport + shared p95 intensity + legend), ring histogram
- `js/positioning-player.js` — Positioning Timeline Player: animated trail playback with transport controls, sub-second interpolation on sparse `trail.t[]`, teleport-segment gap rendering, pulsing current-position dots; exposes `window.VTPositionPlayer`
- `js/all-matches-aggregator.js` — pure client-side cross-match aggregator. Exposes `window.VTAggregate.build(contributions, fileIds, elo)` which sums entries from `data/processed/match_contributions.json` (subset chosen by `fileIds`) into the existing `{meta, career_stats, global_weapon_meta, global_rivalries}` shape PLUS three new top-level blocks: `commander_stats` (rows / head-to-head / most-commanded-against), `faction_stats` (by team slot + win counts), `meta_charts` (maps / duration bands / player counts / submitters / matches over time), and `career_highlights` (24 self-omitting cards). When the optional `elo` arg is provided, the `the_champion` highlight is included. Documented exception to the "no aggregation in JS" rule, made so the All Matches view can scope to the picker-filtered subset. Mirrors `scripts/process_stats.py` `_extract_contribution()` (per-match shape) + the legacy `build_all_matches_aggregate()` (kept as reference). Loaded after `js/positioning-player.js` in `index.html`.
- `scripts/elo.py` — pipeline-side **VTSR-T** (VT Stats Rating — Thug / Thug ELO). Pure module (no I/O); `compute_elo(all_match_data)` returns `(elo_current_dict, elo_history_dict)` ready for `json.dump`. Constants exported at module top: `ELO_ANCHOR = 1500`, `ELO_RATING_SCALE = 2.5`, `ELO_K_LOSS_AVERSION = 0.85`, `ELO_RATING_FLOOR = 1000`, `ELO_FLOOR_TAPER_WINDOW = 150`, `ALPHA = 0.0` (v1 stub), `ALPHA_PVE = 0.5` (v2.3 — fractional PvE weight in the three thug axes), eight-axis `THUG_WEIGHTS` (v2.3 — was `COMBAT_WEIGHTS`; renamed alongside the Combat ELO → Thug ELO architectural rename). v2.4 adds a per-match **commander role adjustment**: new constants `COMMANDER_AXIS_PRIOR` (post-clip-space per-axis priors), `COMMANDER_BASELINE_SHRINKAGE = 30.0`, `COMMANDER_BASELINE_LOCKED_AXES = {"target_lock_pct", "pve_share"}`; new helper `commander_shrunk_baseline(axis, sum, count)`; `compute_performance_index()` takes optional `commander_baseline_snapshot` arg and returns a 4-tuple `(perfs, keys, axis_z, axis_meta)` where the new per-player meta dict carries `{axis: {z_pre_shift, shift, z_post_shift}}` for commander rows on shifted axes. Wired into `scripts/process_stats.py` `main()` after the `match_contributions.json` emit; `load_cache_index()` skip set includes both ELO output files. Algorithm spec: `DEVELOPER_GUIDE.md` §13 (with v2.4 subsection at §13.7.1). Output schemas: `docs/DATA_DICTIONARY.md` §11.
- `vendor/katex/` — KaTeX v0.16.11 (vendored). Used for real LaTeX rendering in the dashboard's VTSR-T methodology modal and the docs methodology section. `katex.min.js` + `katex.min.css` loaded on `index.html`; `contrib/auto-render.min.js` loaded only on `docs.html` (which scans rendered markdown for `$...$` / `$$...$$` delimiters). Only `woff2` fonts are vendored — modern browsers fall back through the `@font-face` source list when `woff` / `ttf` URLs 404. Theme override `:root .katex { color: inherit }` in `css/vtstats-theme.css` ensures equations follow the theme foreground.
- Match Highlights — fixed-slate per-match award catalog. Pipeline: `compute_highlights()` in `scripts/process_stats.py` builds a `highlights` block (12 always-on cards: The Bully, Grim Reaper, Bullet Sponge, Hustler, Sharpshooter, Gunner, Puppeteer, Frenemies, Roadrunner, Pod Goblin, Chris Kyle, Locksmith) from already-built per-match blocks; cards self-omit when their data gates fail. UI: `renderHighlights()` + `HIGHLIGHT_COPY` template dict in `js/app.js` + `.vt-highlights-card` / `.vt-highlight-tile` blocks in `css/vtstats-theme.css`; section markup at `#section-highlights` in `index.html`, immediately above `#section-faction`. Match-global, always-unfiltered (renderer reads `currentData.highlights`, never the filtered `data` view). Bumps `match.schema_version` from `1` → `2`; pre-v2 matches simply have no block (UI hides the section). Full per-card formula table + gates + tiebreakers in `docs/DATA_DICTIONARY.md` §5 `highlights` and the matching glossary entries in §6.
- `raw.html` / `js/raw-browser.js` / `css/raw-browser.css` — Raw Data Browser: isolated standalone page that decodes `*.binpb.gz` client-side (vendored protobufjs-light + native `DecompressionStream`) and renders three tiers per match (raw binpb metadata + download, faithful decoded JSON, processed JSON) in a virtualized tree with domain-aware resolvers (Steam64 → nickname via `header.s64_to_nick`, ODF → `wpn_name` via match-global `odf_map`), search, breadcrumb path, and downloads for all three artifacts. Also ships a virtualized event-stream table (`mode=events` on the decoded tier) with event-type filter chips, dual-handle tick range slider, player-cell click-to-filter, adjacent-pair highlight for `DamageDealt`↔`DamageReceived`, and row-click cross-link to the Replay tab via `?t=<tick>`. Phase 3 adds: hover tooltips on proto-declared field names (sourced from `data/proto-docs.json`), a JSONPath-subset query bar (`$`-prefixed search), and a Reconcile view verifying processed-JSON aggregates against tier-2 event-stream sums for a fixed list of fields. Linked from `index.html`'s match-info banner ("View raw") and the `docs.html` top nav.
- `odf/index.html` / `js/odf-browser.js` / `css/odf-browser.css` + `data/audio/*.wav` — ODF Browser: standalone reference page for browsing the BZ2 Object Definition File (ODF) database. Sidebar carries six category tabs (Vehicle / Weapon / Pilot / Building / Ordnance / Powerup) sourced from `data/odf.min.json` (the same file `js/app.js` resolves weapon names with) plus a fuzzy search input + scrollable `list-group`. Detail view renders one card per ODF class arranged in a 3-up CSS column layout (`.vt-odf-property-cards { column-count: 3 }`, with 2-up @ <1200px and 1-up @ <768px breakpoints) with a per-property fuzzy filter and group tabs (`All` / per-class) when an ODF declares multiple class blocks. Compare mode renders two ODFs side-by-side with hover-synced rows + diff highlighting (`.vt-odf-diff-different` for non-matching values, `.vt-odf-diff-unique` for properties only one ODF has). `.wav`-valued properties get an inline play button that streams from `../data/audio/`. Popovers initialised with `customClass: 'vt-odf-popover'` so they pick up the project theme tokens (mirrors `js/raw-browser.js` line 2756 pattern). Right-click context menu offers "Open in new tab". Keyboard: `↑/↓` cycle ODF list, `←/→` cycle category tabs, `Ctrl+K` focus search, `Esc` clear search, `Esc Esc` reset view. URL routing: `?odf=<name>` deep-links a single ODF, `?compare=<a>,<b>` deep-links a comparison; back/forward navigation works via a `popstate` handler that delegates into `handleCompareState` / `handleODFState`. Exposes `window.browser` so the dynamically-emitted inline `onclick` handlers (audio play, exit-comparison, etc.) keep working without a refactor pass. Linked from the topnav of `index.html`, `docs.html`, `raw.html`. Cross-linked from `js/app.js` `renderKillFeed()` weapon ODF chips: each killer/victim chip is wrapped in `<a class="vt-odf-link" href="odf/index.html?odf=<basename>" target="_blank" rel="noopener">` (with `entry.killer_odf` lowercased + `.odf`-stripped + `encodeURIComponent`'d). The `.vt-odf-link` / `.vt-odf-link-fallback` styles live in `css/vtstats-theme.css` (NOT `css/odf-browser.css`) so they apply on the dashboard. Reference seed: `odf-browser-seed/` (slated for deletion once the port is verified). VSR Build Tree feature is intentionally deferred from this v1; when re-added, fix Bug 7 (hardcoded faction roots `ibrecy_vsr` / `ebrecym_vsr` / `fbrecy_vsr` per `odf-browser-seed/ODFBrowser_TechSpec.md` §9) and re-vendor the 3 faction logos.
- `scripts/extract_proto_docs.py` / `data/proto-docs.json` — build script + generated artifact. Parses inline `//` comments from `scripts/statsgate.proto` into a flat `{"MessageName.fieldName": "comment"}` dict (camelCase keys matching protobufjs's `toObject` output). Invoked as part of the main pipeline run. Consumed by `js/raw-browser.js` for field-name hover tooltips in the Decoded tier.
- `scripts/build_map_registry.py` / `data/map-registry.json` / `data/maps/` — build script + generated registry + fetched map assets. **Primary** source: vendored `data/vsrmaplist.json` (BZCC-Website upstream), which supplies `Name` / `Description` / `Author` / `Image` URL / `Size.{size,baseToBase,formattedSize}` / `Pools` / `Loose` / `Tags`. **Per-map enrichment**: `https://gamelistassets.iondriver.com/bzcc/getdata.php` for `netVars` (team-name slot vars `svar1`/`svar2` etc.) plus richer title/description fallback. **Legacy fallback**: baked-in `VSR_MAP_DATA` from `js/bz2api.js` for author / size / baseToBase when a map is missing from `vsrmaplist.json`. Downloads images to `data/maps/<mapFile>.png` (iondriver URL primary, vsrmaplist `Image` URL fallback — both content-addressed `assets/<sha>.png` on iondriver host). Idempotent: re-runs are zero-network when maps are already cached. Vsrmaplist-sourced fields (`pools` / `loose` / `tags` / `formatted_size`) get *additively backfilled* onto already-cached `data/maps/<mapFile>.json` files without forcing a refetch. Invoked from the pipeline run *before* the manifest is written so that `manifest[i].name` in `data/processed/matches.json` can be resolved from `registry[<key>].title` (with iteratively-stripped `XYZ: ` prefixes — e.g. `"VSR: Ancient Hills"` → `"Ancient Hills"`, `"ST: VSR: TVD: Ebola"` → `"Ebola"`). Falls back to the raw `.bzn`-stripped filename when the registry has no title for a map. The pipeline calls `build_registry(map_mod_entries=...)` with an in-memory `(map_file_key, config_mod)` list so the builder doesn't need to re-read `matches.json`; the standalone CLI invocation (`python scripts/build_map_registry.py`) still works via the `discover_map_files()` fallback. Consumed by `js/app.js` for hero stats + the Map Info Modal + `js/positioning-charts.js` for heatmap overlays.
- `data/vsrmaplist.json` / `scripts/refresh_vsrmaplist.py` — vendored upstream BZCC-Website map index + one-shot refresh helper. Source: `https://battlezonescrapfield.github.io/BZCC-Website/data/maps/vsrmaplist.json`. Static catalog (143 maps as of vendor) of every VSR map: `Name` / `File` / `Image` URL / `Pools` / `Loose` / `Author` / `Description` / `Tags` / `Size.{formattedSize,size,baseToBase,binarySave}`. Promoted to the **primary** metadata source for `scripts/build_map_registry.py`; iondriver `getdata.php` is still called per-map for `net_vars` enrichment (team-name slot vars) and richer title/description fallback. The refresh script (`python scripts/refresh_vsrmaplist.py`) fetches + parse-validates + atomically rewrites the vendored copy; run manually when a new VSR map ships and isn't covered. Coverage as of vendor: 34/34 maps in `data/processed/matches.json`. Maps absent from `vsrmaplist.json` cleanly fall through to the legacy iondriver + `VSR_MAP_DATA` path.
- `js/bz2api.js` — vendored BZ2API library: baked-in VSR map catalog (`VSR_MAP_DATA`, **legacy fallback only** since `data/vsrmaplist.json` was vendored), CORS-proxied iondriver API client, lobby session parser. Still used at runtime by `js/app.js`'s `getMapMeta()` as a last-resort fallback when `data/map-registry.json` is missing fields; deletion deferred until `vsrmaplist.json` proves stable. Sourced from [sevsunday/bz2vsr](https://github.com/sevsunday/bz2vsr).
- `vendor/protobufjs/protobuf.min.js` / `vendor/protobufjs/statsgate.proto.json` — vendored protobufjs v7 light build + generated descriptor consumed by `raw-browser.js` to decode `ClientStatSession` messages in the browser.
- `scripts/verify_proto_decode.mjs` — schema-migration verification tool: one-off Node script that decodes a real `*.binpb.gz` with protobufjs + the generated descriptor and prints header/event-count summary for comparison against the Python pipeline. Requires `npm i --no-save protobufjs@7` before running.
- `scripts/audit_sentinel_events.mjs` — permanent debugging utility that scans every `data/sessions/**/*.binpb.gz` and histograms `DamageDealt` / `DamageReceived` events exceeding the `SENTINEL_DAMAGE_THRESHOLD` (> 1e6). Primary metric `total_sentinel_pairs` must agree with the All Matches aggregate's `meta.total_sentinel_damage_dropped` (built client-side by `js/all-matches-aggregator.js` from `data/processed/match_contributions.json`). Writes reports into gitignored `_investigation/output/`.
- `scripts/dump_events_window.mjs` — general-purpose event-stream dumper; decodes a single `*.binpb.gz` and prints events in a `[tick_min, tick_max]` window. Useful for investigating any time-localized anomaly, not just sentinels.
- `docs/DATA_DICTIONARY.md` §7 "Sentinel Damage Filter" — first-class reference for the `amount > 1e6` sentinel damage filter. Evidence chain, BZCC engine struct layout, Ghidra decompile of the `DAMAGE_TYPE_UNKNOWN` force-kill pathway, bit-pattern verification, and the proposed upstream `DamageType` enum schema enhancement. Linked from `.cursor/rules/data-schema.mdc` and `DEVELOPER_GUIDE.md`. The companion `docs/DATA_DICTIONARY.md` §8 "UnitDestroyed Classification & Powerup Economy" covers the four-way classification + powerup-pickup semantics.
- Team Faction Detection (ISDF / Hadean / Scion) — `detect_team_factions()` in `scripts/process_stats.py` derives each team's faction from per-slot ODF signals: Algorithm A (starting ship: first non-empty `PlayerState.odf` per slot) + Algorithm B (event-stream votes: first-letter votes from every team-attributed vehicle/structure/pilot ODF across `BulletHit`, `UnitDestroyed`, `UpdateTick`, `PickupPowerup`, `UnitSniped`). Output at `match.team_factions = {"1": {"code", "name"} | null, "2": ...}`. Match-global, always-unfiltered. Bumped `match.schema_version` from `2` to `3`. Pre-v3 matches simply have no block (UI hides the badge). Renderer: `renderFactionScoreboard()` in `js/app.js` injects `.vt-faction-badge[data-faction-code]` next to each team-leader name. Kill-feed ODF chips now resolve through `currentData.odf_map` (built by the pipeline) and are omitted entirely when the ODF is empty/unresolved. Full per-source vote table + edge cases: `docs/DATA_DICTIONARY.md` §9.

## Key Conventions

- `scripts/statsgate.proto` is the single source of truth for the **current (v2)** raw data schema. All docs, rules, and pipeline code must match it. When it changes, regenerate `scripts/statsgate_pb2.py` and `vendor/protobufjs/statsgate.proto.json`; everything downstream updates.
- **Dual-descriptor decode (v1/v2 corpus split).** The pipeline transparently supports both the legacy schema (`scripts/statsgate_v1.proto`, separate `DamageDealt`+`DamageReceived` events) and the current schema (unified `DamageDealt` + `Race` enum + `shutdown_requested` + `BulletHit.distance_to_target`). `load_session()` returns `(session, schema)` where `schema` is `"v1"` or `"v2"`; the consumer loop normalizes both paths to a single `DamageEvent` shape so downstream aggregators are schema-agnostic. The detected schema is stamped on `match.proto_schema_version` (debugging telemetry only — UI never branches on it). The same try-v2/fallback-v1 dance happens in the Raw Data Browser. Maintain byte-for-byte parity for v1 corpus on any schema-related change — the only allowed diffs are version bumps and new optional fields.
- All data processing happens in the Python pipeline (`scripts/process_stats.py`), never in browser JavaScript — with one **documented exception**: `js/all-matches-aggregator.js` performs pure summation over `data/processed/match_contributions.json` to produce the All Matches view's aggregate. This exists so the picker's facet filters (player count, duration band, players, role, etc.) can scope the aggregate without per-match round trips. Per-match aggregation (leaderboards, weapon meta, rivalry matrices) still happens in the pipeline; only the cross-match summation moved client-side.
- All dependencies are vendored locally in `vendor/` — no CDN usage.
- All colors come from CSS custom properties (`--kb-*`) — zero hardcoded colors in HTML or JS. Visual effects use `--vt-*` variables.
- **Geist** (vendored in `vendor/fonts/`) is the project typeface. Geist Sans for body/UI, Geist Mono for stat values.
- All dashboard styles live in CSS files — **zero inline `<style>` blocks** in HTML.
- Dashboard uses tabbed navigation with lazy rendering — charts render on first tab activation, not on page load.
- Global player filter is client-side only — `getFilteredData()` derives filtered views from loaded JSON using the same lazy re-render pipeline as match switching. `weapon_meta` is recomputed from `leaderboard[].weapon_breakdown` when filtering. `kills.by_vehicle` is always unfiltered. No pipeline changes needed.
- Match picker filter is **separate** from the per-match player filter. `pickerState` (in `js/app.js`, persisted in sessionStorage as `vt.picker.filters.v2`) drives which manifest entries the All Matches aggregate covers — not what shows inside a single match. Facets: duration band (radio), player count (multi), submitter (multi), full-roster Players (multi) with two orthogonal toggles: Match-mode (`Any of these` vs `All of these`) and Role (`Any role` / `Commander` / `Thug`). Role is derived per-match: a name in `team_leaders` is the match's commander; otherwise (still in `players[]`) it's a thug. When the user is on the All Matches view, a chip toggle debounce-reruns `loadAllMatches()` so the career table / radar / global meta reflect the filtered subset. The All-Matches-view banner above the aggregate surfaces "Aggregate of K of N matches matching the active filters" + a Clear-filters shortcut.
- Career-roster threshold: `js/all-matches-aggregator.js` enforces a hard-coded `MIN_CAREER_MATCHES = 5` after summation — `career_stats[]` rows with fewer matches *in the current scope* are dropped, and `global_rivalries[]` is cascade-filtered to the kept names. `global_weapon_meta` and `meta.*` totals are unaffected; per-match views (Player Leaderboard, kill feed, etc.) are unaffected. The threshold is surfaced via `meta.min_career_matches` (read by UI labels — never duplicate the constant) and `meta.players_dropped_by_min_matches` (count of pruned rows). Any new cross-match aggregate that surfaces players in the All Matches view must respect the threshold by reading from the post-prune `career_stats[]` (or by checking `keptNames` if it builds in parallel).
- Processed JSON is the source of truth for the browser; the proto is the source of truth for the pipeline.
- Players are identified by Steam64 IDs. Header maps: `s64_to_nick`, `teamnum_to_s64`, `s64_to_teamnum`.
- Faction determined by slot convention: slots 1-5 = Team 1, slots 6-10 = Team 2.
- Session data lives in `data/sessions/<username>/*.binpb.gz`, organized by submitter.
- Movement Profile (0-100 `activity_score` per player, career-aggregated) is derived from `UpdateTick` position samples in the pipeline. Higher = more active / covered more map. See `.cursor/rules/data-schema.mdc` for the formula and band thresholds, `docs/DATA_DICTIONARY.md` for the full positioning JSON schema.
- T-Key Usage (`positioning.players[].metrics.target_lock_pct`, 0-1 ratio) is captured from `PlayerState.has_target` (the T-key activates a tap-to-toggle target mode against the nearest enemy; the boolean reflects whether a target lock is currently active, not key-press state or aim placement). Availability is gated by the match-global `has_target_lock_data` flag (mirrored on `positioning`, `match`, and manifest entries) which is `false` for pre-schema sessions and matches where no player ever activated target mode. Unlike `activity_score`, `target_lock_pct` is absolute (cross-match comparable) and averaged directly in `career_stats[].mean_target_lock_pct`. Rendered on the 8th "T-Key Usage" axis of the Player Performance Radar.
- Match Highlights (`match_data.highlights`, schema_version 2) is a fixed-slate, always-on per-match award catalog (12 cards). Pre-computed in `compute_highlights()`; the dashboard reads `currentData.highlights` and **never** narrows under `filterState` — same passthrough contract as `kills.by_vehicle`. Cards self-omit when their data gates fail (positioning / pickups / T-key / snipe gates), so the section gracefully degrades on legacy / low-roster matches. Bumps `match.schema_version` from `1` → `2`; pre-v2 matches simply have no `highlights` block (UI hides the section).
- Team Factions (`match.team_factions`, schema_version 3): per-team ISDF / Hadean / Scion label derived from per-slot ODF first-letter votes. Same passthrough contract as `kills.by_vehicle` and `highlights` — match-global, always-unfiltered. Pre-v3 matches simply have no block (UI hides the badge).
- Match Winner (`match.winner`, schema_version 3): inferred match outcome from the cleaned kill feed via a toggle model on recycler/factory destructions (`compute_match_winner()` in `scripts/process_stats.py`). Outcomes: `clean_win` / `contested` / `unclear`. Always emitted; `team` / `loser` are `null` for unclear. Same passthrough contract as `team_factions` — match-global, always-unfiltered. Renderers read from `currentData.match.winner` rather than the (filterable) `kills.feed` so a player filter can't corrupt the result.
- VTSR / **VTSR-T** (`data/processed/elo_current.json`): **VTSR-T** is the thug-focused rating emitted by `scripts/elo.py` (fine-tuned ELO-style updates over an **eight-axis thug composite**). The headline **VTSR-T** field (`vtsr` in JSON) equals **Thug ELO** (the new v2.3 name for what was Combat ELO) while `α = 0`. Anchored at 1500, soft floor at 1000 with 150-pt linear taper. Asymmetric K-factor with loss aversion 0.85 and provisional-style K decay. **Corpus-wide / picker-filter-unaware** — picker narrows the displayed roster only, never recomputes ratings against the filtered subset (filter-contract.mdc has the formal contract). Wins ELO is stubbed at the anchor for every player so the blend math runs through unchanged until winner-attestation data lands. Pipeline-emitted, corpus-wide, JS aggregator passes through unchanged. 404-safe in the dashboard (`window.__vtElo === null` → VTSR-T card hidden, career-table Tier+VTSR-T cells render `—`). Full algorithm in `DEVELOPER_GUIDE.md` §13; output schemas in `docs/DATA_DICTIONARY.md` §11. **v2.4 (current)**: per-match commander role adjustment on the unified VTSR-T leaderboard. Each commander match-row (`is_commander: true` on the source leaderboard row) gets a per-axis additive shift in **post-clip space** (after `clip(z, -2, +2) / 2`) then re-clipped to `[-1, +1]`. Asymmetric design: 4 audit-derived priors (`mobility -0.488`, `thug_kill_rate -0.164`, `net_damage_share -0.131`, `thug_efficiency -0.106`) ride a shrunk rolling baseline (`COMMANDER_BASELINE_SHRINKAGE = 30`) so live data takes over the seed as the corpus grows; 2 hand-tuned priors LOCKED at seed (`target_lock_pct -0.10` cushion, `pve_share -0.05` reward boost — `COMMANDER_BASELINE_LOCKED_AXES = {"target_lock_pct", "pve_share"}`); 2 axes role-blind (`thug_accuracy`, `snipe_bonus`). Commanders defying thug-tier expectations (high mobility, high net damage) earn extra rating credit naturally because the bar drops. New `commander_axis_prior` / `commander_baseline_shrinkage` / `commander_baseline_locked_axes` / `commander_baseline_observed` (with `locked: bool` per axis) blocks on `elo_current.json`; new per-rating `matches_as_commander` / `matches_as_thug`; new optional `axis_contributions_meta` (`{axis: {z_pre_shift, shift, z_post_shift}}`) on commander deltas in `elo_history.json` — flat `axis_contributions` shape unchanged so existing JS rendering works. Schema bumps: `ELO_SCHEMA_VERSION 4 → 5`, `PIPELINE_VERSION 14 → 15`. **Pre-v2.4 `peak_vtsr` values are no longer comparable** — corpus re-rated. **v2.5 (current) — row-level exclusion gates**: two new boolean flags on every per-match `leaderboard[]` row set by `scripts/process_stats.py` — `is_campod` (player spent > `CAMPOD_MAX_SHARE = 0.25` of match wall-clock in any of the four player-flyable camera-pod ODFs `{evcamr_vsr, fvcamr_vsr, ivcamr_vsr, camerapod_vsr}.odf`) and `is_low_activity` (event-stream presence window `last_event_tick - first_event_tick` covered < `LOW_ACTIVITY_MIN_PRESENCE = 0.75` of match duration — catches late joiners AND mid-match DCs in one window check). Rows where either flag is true are **omitted** from the rated lobby in `compute_performance_index` (no per-axis contribution, no delta in `elo_history`, no `matches_played` bump — pure omission, **zero penalty**) and skipped by `js/all-matches-aggregator.js` in career rollups. Two new pool-level counters on `elo_current.json`: `rows_excluded_campod` / `rows_excluded_low_activity`. Two supporting numeric fields (`campod_share`, `presence_window_sec`) power UI hover tooltips. Per-match dashboard keeps excluded rows VISIBLE in the Player Leaderboard with `Campod` / `Partial` badges + 55% opacity rows for transparency. No algorithm changes — axis weights, priors, K-factor, loss aversion, floor taper, alpha-blend all unchanged. Schema bumps: `ELO_SCHEMA_VERSION 5 → 6`, `PIPELINE_VERSION 15 → 16`, `match.schema_version 5 → 6`. Both thresholds tunable post-ship without a schema bump (just bump `PIPELINE_VERSION`). Full per-row schema in `docs/DATA_DICTIONARY.md` §10.2. **v2.3 (retained)**: alpha-blended thug composite + Combat ELO → Thug ELO rename — three changes: (1) the three "thug" axes (`thug_kill_rate`, `thug_accuracy`, `thug_efficiency`) credit PvE work at `ALPHA_PVE = 0.5` (locked default; surfaced as `alpha_pve` in elo_current.json — tunable post-ship without a schema bump). (2) `thug_accuracy` is weapon-normalized against the lobby's per-weapon baseline. (3) `pve_share` replaces v2.2's narrower `structure_share` — covers all enemy non-human damage (sources from `personal.pve_dealt`). Architectural rename `Combat ELO → Thug ELO` reflects that VTSR-T's combat-skill component is specifically thug-flavored; sets up future VTSR-C (commander) sibling. JSON field rename `combat_elo → thug_elo` on every `ratings[]` row; Python `COMBAT_WEIGHTS → THUG_WEIGHTS`; math notation `R^C → R^T`. The `axis_means` block on each rating row + `axis_contributions` block on each `elo_history.deltas[]` (added in v2.3) power the VTSR-T leaderboard's Strong-axes tooltip and Last-delta breakdown popover. v2.3 bumped `match.schema_version` 3 → 4 (per-match leaderboard rows gained personal.pvp_kills/pve_kills/pvp_deaths/pve_deaths/pvp_shots_hit, weapon_breakdown[w].pvp_hits, loadout block, per_class_combat list).
- Career Highlights (`career_highlights` block on the All Matches aggregate output): client-side counterpart to per-match Match Highlights. 24 cards built by `buildCareerHighlightsInline()` in `js/all-matches-aggregator.js` — 12 `career_*` siblings of the per-match catalog plus 12 cross-match-only originals (`the_champion` / `the_veteran` / `the_workhorse` / `the_carry` / `the_anchor` / `isdf_loyalist` / `hadean_loyalist` / `scion_loyalist` / `the_diplomat` / `map_master` / `streak_king` / `the_polymath`). Cards self-omit when their data gates fail (e.g. Sharpshooter requires `total_shots_fired >= 1000`). Renderer is the existing `renderHighlights()` in `js/app.js` with a new `mode` parameter (`'match'` | `'career'`) that selects between MATCH and CAREER COPY/UNITS tables and between the two DOM containers (`#section-highlights` / `#section-career-highlights`).
- Commanders + Meta tabs on the All Matches view: `#all-tab-commanders` + `#all-tab-meta` panes added in this overhaul. Three Commanders cards (leaderboard / head-to-head / faction-picks chart) + six Meta charts (maps stacked bar, faction by team slot dual donut, faction win rate, duration histogram, player count, matches over time). Both panes lazy-render via `registerTabRenderer()` from the aggregator's new `commander_stats` / `faction_stats` / `meta_charts` blocks. Win % cells degrade to em-dash with explanatory tooltips below the 5-determined-match floor. Faction palette tokens (`--kb-faction-i` / `-e` / `-f`) live in `css/vtstats-theme.css` and are shared by the badges, the faction-picks chart, and all Meta faction visualisations.
- KaTeX (vendored at `vendor/katex/`, v0.16.x): real LaTeX rendering for the VTSR-T methodology modal on the dashboard and the methodology section in `docs.html`. Loaded synchronously on `index.html` (pre-rendered via `katex.renderToString()` with the HTML cached in a module-local `vtsrTooltipHtmlCache`); auto-render contrib loaded only on `docs.html`. Same source string round-trips between GitHub's native LaTeX renderer and KaTeX (delimiters: `$...$` / `$$...$$`). When adding new math anywhere, mirror this approach: pre-render where possible (avoid FOUC inside Bootstrap tooltips), reuse the `.vt-katex-tooltip` Bootstrap-tooltip custom class.
- ODF Browser is the project's fourth standalone page (alongside dashboard / docs / raw browser). It's a pure read-only consumer of `data/odf.min.json` + `data/audio/*.wav`; it does NOT participate in the pipeline cache key, the picker filter contract, or the per-match player filter — there's no "filtered ODF view" notion. The dashboard cross-links into it from `js/app.js` `renderKillFeed()` weapon ODF chips via `<a class="vt-odf-link" href="odf/index.html?odf=<basename>" target="_blank">`. `.vt-odf-link` styling lives in `css/vtstats-theme.css` (loaded by the dashboard); all other `.vt-odf-*` styling lives in `css/odf-browser.css` (loaded only by `odf/index.html`). VSR Build Tree feature deferred from v1; when re-added, fix Bug 7 (hardcoded faction roots `ibrecy_vsr` / `ebrecym_vsr` / `fbrecy_vsr` per `odf-browser-seed/ODFBrowser_TechSpec.md` §9) and re-vendor the 3 faction logos.
- Pipeline invocation: `python scripts/process_stats.py` is the single entrypoint and runs in **sync-default + incremental** mode. It pulls the sibling `statsgate/` git clone (`--ff-only`), additively mirrors any new `.binpb.gz` files into `data/sessions/` (never deletes, never overwrites — local-only submitter folders are preserved), then processes only matches whose source is new or has changed size since the last run. Cache key is `(match.submitter, match.source_file, match.source_size_bytes)` plus a top-level `PIPELINE_VERSION` constant (currently `1`) for code-change invalidation. **Bump `PIPELINE_VERSION` whenever `process_match()` or any helper it transitively calls (positioning, highlights, weapon meta, rivalry matrices, `_extract_contribution`, etc.) changes output semantics** — orthogonal to `match.schema_version` (frontend contract). Flags: `--no-sync` (offline, skip git pull + mirror), `--sync-only` (fetch then exit, no processing), `--force` (ignore cache, reprocess all). Missing `statsgate/` is a soft-skip (the manual-drop-only workflow needs no flag); failed `git pull` is a hard-fail with a `--no-sync` hint.

## When Schema Changes

1. Replace `scripts/statsgate.proto` with the new version
2. Follow `.cursor/rules/schema-migration.mdc` checklist
3. Update pipeline, JSON output, JS rendering, `data-schema.mdc`, and `DEVELOPER_GUIDE.md` — in that order
4. Regenerate the raw-browser descriptor: `npx pbjs -t json scripts/statsgate.proto > vendor/protobufjs/statsgate.proto.json`
5. Verify the Node/browser decode path still matches the Python pipeline by running `scripts/verify_proto_decode.mjs` (see the script header for setup) before shipping the new schema
